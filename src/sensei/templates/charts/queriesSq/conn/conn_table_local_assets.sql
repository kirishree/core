select count(*) as total,sum(src_nbytes) as src_nbytes,sum(dst_nbytes) as dst_nbytes,sum(src_npackets) as src_npackets,sum(dst_npackets) as dst_npackets,count(distinct src_hostname) as dc_src_hostname,count(distinct app_name) as app_name,src_hostname from conn_all where start_time>__GTE__ and start_time<__LTE__ and src_dir="EGRESS" __WHERE__ group by src_hostname order by 1 desc limit __SIZE__